import { BadgeAwardDirective } from './badge-award.directive';

describe('BadgeAwardDirective', () => {
  it('should create an instance', () => {
    const directive = new BadgeAwardDirective();
    expect(directive).toBeTruthy();
  });
});
